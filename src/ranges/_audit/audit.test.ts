// Auditoria de ranges — arquivo de teste separado do build
import { describe, it, expect } from 'vitest';

describe('audit ranges', () => {
  it('placeholder', () => {
    expect(true).toBe(true);
  });
});
