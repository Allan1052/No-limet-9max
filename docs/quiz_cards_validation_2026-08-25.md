# Validação visual do pacote de 10 quizzes

A folha de revisão mostrou que os dez cards usam a mesma linguagem visual, proporção 4:5, logo oficial, domínio e CTA coerente com o tipo de decisão.

O Card 1 foi conferido em tamanho maior. O título `9♠ 7♠ NO UTG`, a pergunta `ABRE OU FOLD?`, o CTA `COMENTA: ABRE OU FOLD? ANTES DA RESPOSTA`, as duas cartas e o rodapé estão visíveis e dentro da área segura. A carta 9♠ mostra nove pips e a 7♠ mostra sete pips, sem terceira carta. A assinatura `feito por um recreativo` aparece uma única vez no topo.

Ainda falta conferir individualmente pelo menos um spot de defesa, um spot com três limpers e um spot com múltiplas ações (`CALL / 4-BET / FOLD?`) antes de considerar o conjunto pronto.

O Card 3 foi conferido em tamanho maior. O cenário `UTG abriu · você no MP`, a pergunta `CALL OU FOLD?`, o CTA correspondente e as cartas 6♠/5♠ estão coerentes.

O Card 7 também foi conferido. O spot `3 LIMPERS · VOCÊ: CO` está destacado, a pergunta usa `ISO OU LIMPA?` e o CTA repete exatamente a decisão. A♠ e J♠ aparecem separadas e com índice/naipe legíveis.

Ainda falta conferir o Card 10, que possui a decisão de três caminhos `CALL / 4-BET / FOLD?`, antes da aprovação final do conjunto.
